image:
  app: ${APP_IMAGE_NAME}:${GITHUB_SHA}
  migrations: ${MIGRATIONS_IMAGE_NAME}:${GITHUB_SHA}

openTelemetry:
  endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT}
  headers: ${OTEL_EXPORTER_OTLP_HEADERS}

postgres:
  db: woog
  user: woog
  host: backend-db

sentry:
  dsn: ${SENTRY_DSN}
